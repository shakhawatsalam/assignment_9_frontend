export enum tagTypes {
  user = "user",
  service = "service",
  booking = "booking",
  closedays = "closedays",
  day = "day",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.service,
  tagTypes.booking,
  tagTypes.closedays,
  tagTypes.day,
];
