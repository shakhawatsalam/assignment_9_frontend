export enum tagTypes {
  user = "user",
  service = "service",
  booking = "booking",
  closedays = "closedays",
  day = "day",
  timeslots = "timeslots",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.service,
  tagTypes.booking,
  tagTypes.closedays,
  tagTypes.timeslots,
  tagTypes.day,
];
