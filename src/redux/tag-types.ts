export enum tagTypes {
  user = "user",
  service = "service",
  booking = "booking",
  closedays = "closedays",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.service,
  tagTypes.booking,
  tagTypes.closedays,
];
